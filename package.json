{
  "name": "nuxt-auto-save",
  "displayName": "Nuxt Auto-Save",
  "description": "Silently auto-saves dirty .nuxt files when opened in VS Code.",
  "version": "0.0.2",
  "publisher": "seaniod",
  "engines": {
    "vscode": "^1.100.0"
  },
  "main": "./extension.js",
  "activationEvents": [
    "*"
  ],
  "contributes": {},
  "scripts": {
    "lint": "eslint .",
    "pretest": "pnpm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.100.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "eslint": "^9.25.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2"
  },
  "license": "MIT"
}